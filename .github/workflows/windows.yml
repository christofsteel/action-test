# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: windows-latest

    # Steps represent a sequence of tasks that will be executed as part of the job    
    steps:
     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache MPV download
        uses: actions/cache@v3
        with:
          path: mpv-x86_64-windows-msvc.zip
          key: ${{ runner.os }}-mpv-${{ hashFiles('**/mpv-x86_64-windows-msvc.zip') }}

      - name: Download and extract latest MPV
        run: |
          if (-Not (Test-Path -Path mpv-x86_64-windows-msvc.zip)) {
            Invoke-WebRequest -Uri https://nightly.link/mpv-player/mpv/workflows/build/master/mpv-x86_64-windows-msvc.zip -OutFile mpv-x86_64-windows-msvc.zip
          }
          Expand-Archive -Path mpv-x86_64-windows-msvc.zip -DestinationPath .      
